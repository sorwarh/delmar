/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'tailwindui.com'], // Add the allowed domains here
    },
  };
  
  export default nextConfig;
  